<div class="contents row">
  <%= form_for(@tweet) do |f| %>
    <h3>
      新規投稿
    </h3>
    <%= f.text_field :image, placeholder: "Image URL"  %>
    <%= f.text_area :text, placeholder: "text" %>
    <%= f.submit "投稿" %>
  <% end %>
</div>
